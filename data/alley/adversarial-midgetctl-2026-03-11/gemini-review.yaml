```yaml
role: adversarial-reviewer
model: gemini-1.5-pro
verdict: fail
findings_count: 3
findings:
  - id: F-1
    severity: medium
    category: gap
    location: tests/test-steer.sh:162
    claim: midgetctl steer "injects message into agent context (infrastructure)" and "The steer-watcher process inside the container picks up JSON message files... and writes them to a named pipe that the agent reads as stdin." (bin/midgetctl:160-165)
    reality: The deterministic test (T3.3) verifies that `steer-watcher` successfully reads a JSON file and writes its content to a named pipe, and that a generic `cat` command can read from this pipe. However, it does not actually run an agent process (e.g., `claude --input-format stream-json`) to confirm that an agent successfully connects to, reads from, and processes these messages from the pipe as a "user turn." While `test-steer-live.sh` partially addresses this by running a live agent, the *deterministic* test itself still has this gap, proving only the plumbing up to the pipe and a generic consumer, not the full claim regarding agent interaction.
    evidence: |
      - bin/midgetctl:160: "midgetctl steer <container> <msg> inject message into agent context (infrastructure)"
      - bin/midgetctl:163-165: "The steer-watcher process inside the container picks up JSON message files from /opt/jobs/steer/ and writes them to a named pipe that the agent reads as stdin."
      - tests/test-steer.sh:162-169: `docker exec -d "$CONTAINER" bash -c "cat /tmp/steer/agent.pipe > /tmp/pipe-output.txt"` (This uses `cat` as a pipe consumer, not an actual agent with `stream-json` input.)
  - id: F-2
    severity: high
    category: claim-not-proven
    location: docs/diagrams/value-proposition.md:7
    claim: Midgets enable "E2E testing and user behaviour simulation" requiring a full visual stack (Xvfb, fluxbox, Chrome, xdotool, OCR, tesseract). It is claimed that "The midget stack (steer + drive + tesseract) gives an agent the same interface a QA engineer has."
    reality: The reviewed code (`bin/midgetctl`, `steer/steer-watcher`) and tests (`test-steer.sh`, `test-steer-live.sh`) focus exclusively on text-based steering mechanisms (pipe-based JSON injection and file-based instruction). There is no code or testing evidence provided for the `drive` or `tesseract` components, nor any demonstration of the claimed visual interaction, browser automation, or OCR capabilities. This core value proposition remains entirely unproven by the provided implementation and test suite.
    evidence: |
      - docs/diagrams/value-proposition.md:7-14: "E2E testing and user behaviour simulation. ... The midget stack (steer + drive + tesseract) gives an agent the same interface a QA engineer has."
      - The absence of any code or tests related to Xvfb, fluxbox, Chrome, xdotool, OCR, `drive`, or `tesseract` in the provided files.
  - id: F-3
    severity: medium
    category: claim-not-proven
    location: docs/diagrams/value-proposition.md:36
    claim: "Visual audit via VNC. You can watch what the agent is doing in real time by connecting a VNC viewer to port 5900. The agent's role name appears in the fluxbox toolbar."
    reality: While `midgetctl watch` is implemented to launch a VNC viewer, and `midgetctl run` supports VNC enablement, there are no tests that demonstrate a successful connection to a live midget's VNC display, nor any verification of the content displayed (e.g., the agent's role name in the fluxbox toolbar). The claim of "visual audit" and specific display features remains an assertion without direct proof in the testing suite.
    evidence: |
      - docs/diagrams/value-proposition.md:36-39: "Visual audit via VNC. You can watch what the agent is doing in real time... The agent's role name appears in the fluxbox toolbar."
      - The absence of any `test-steer*.sh` tests that involve `midgetctl watch` or verification of visual output.
summary: The `midgetctl` steering module provides both infrastructure-enforced and prompt-based steering mechanisms, and the core plumbing for both (file creation, JSON formatting, pipe delivery for `steer`, and file appending for `signal`) is well-tested in `test-steer.sh`. The live tests in `test-steer-live.sh` successfully demonstrate an agent acting on both types of steering, bridging the gap between the deterministic plumbing and actual agent compliance. However, significant claims in `docs/diagrams/value-proposition.md` regarding E2E visual simulation, including components like `drive` and `tesseract`, and the verifiability of a "visual audit via VNC," remain entirely unproven by the provided code and tests. The current testing focuses narrowly on the text-based steering mechanisms, leaving a substantial portion of the high-level value proposition unvalidated.
```
